                //create array
                const arr=[23,78,46,15,65,55];
                //sorting array in decending order
                arr.sort();
                arr.reverse();
                console.log('Array in decending order:',arr.reverse());
                
                //using built in funtions
                const num=7.47;
                const round=Math.round(num);
                const ceil=Math.ceil(num);
                const floor=Math.floor(num);

                console.log('Decible number:',num);
                console.log('number after rounding:',round);
                console.log('number after ceil:',ceil);
                console.log('number after floor:',floor);
   