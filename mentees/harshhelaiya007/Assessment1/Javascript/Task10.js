const normalArrowFn = (firstName, lastName) => {
    return firstName + ' ' + lastName
}

console.log(normalArrowFn("Harsh", "Helaiya"));

const userProfile = {
    name: "Harsh",
    age: 21,
    interest: ["reading", "travel", "swimming"]
}

console.log(userProfile.name, userProfile.age, userProfile.interest)