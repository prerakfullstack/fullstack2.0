const normalArrowFn = (firstName, lastName) => {
    return firstName + ' ' + lastName
}

console.log(normalArrowFn("Akshay", "Gohil"));

const userProfile = {
    name: "Akshay",
    age: 21,
    interest: ["code", "travel", "gym"]
}

console.log(userProfile.name, userProfile.age, userProfile.interest)