function sumThreeNumbers(numbers) {
    const [num1, num2, num3] = numbers; // Deconstructing the array
    return num1 + num2 + num3;
}


const numbersArray = [10, 20, 30];
console.log(sumThreeNumbers(numbersArray));


