Explanation of the Code:

Header:

The <header> element has a green background color (#4CAF50) and white text.

Div with Border:

The <div> element with the class bordered-div has a solid black border (2px solid black), padding, and a light gray background color.

Section with Background Image:

The .background-image class applies a background image to the section. The image is set to cover the entire section while being centered.
The height is fixed at 300px, and the text is centered both vertically and horizontally, with a white color and a shadow for better visibility against the image.