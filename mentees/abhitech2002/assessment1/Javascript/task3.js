// Array
const countries = ['India', 'America', 'Japan', 'Spain', 'London']; // Declaration

// For loop
for (let i = 0; i < countries.length; i++) {
    console.log('The countries are:', countries[i])
}

function multiplication(num) {
    for (let i = 1; i <= 10; i++) {
        console.log(`${num} * ${i} = ${num * i}`)
    }
}

// Logging
console.log('ultiplication of the num');

console.log(multiplication(10))